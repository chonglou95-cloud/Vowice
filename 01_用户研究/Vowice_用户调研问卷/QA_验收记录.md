# Vowice 用户研究问卷验收记录

验收日期：2026-06-18

## 已完成检查

| 检查项 | 结果 |
|---|---|
| HTML5 文档结构 | 通过，`html5lib` 报告 0 个解析错误 |
| 内嵌 JavaScript 语法 | 通过，Node.js `--check` 无错误 |
| 问卷长度 | 13 题：11 题必答、2 题选填，目标用时 5–8 分钟 |
| 分步结构 | 4 步：使用习惯、声音与玩法、主动与记忆、顾虑与期待 |
| 多选限制 | 场景、声音特质、玩法、记忆和顾虑均设置最多选择数 |
| 排他选项 | “暂时没有”“不想调整”等选项会与其他选项互斥 |
| 草稿保护 | 未提交答案保存在当前浏览器，提交成功后清除草稿 |
| 本地模式 | 支持本地保存及 `?admin=1` 导出 JSON/CSV |
| 正式模式 | 支持通过 Supabase REST API 只写提交 |
| 数据最小化 | 不收姓名、联系方式、设备信息或主动读取 IP |
| 数据库权限 | 匿名角色只有 `INSERT` 权限，无读取、修改、删除权限 |

## 尚未完成的动态检查

应用内浏览器安全策略拒绝访问本地 `file://` 页面，因此本轮未能完成真实浏览器中的点击流程、桌面截图和手机截图。正式投放前需在部署后的 HTTPS 地址补做：

1. 从开始页完整提交一次。
2. 检查四步前进、后退和必答提示。
3. 在 390 × 844 与 1440 × 1000 视口检查文本、选项和底部按钮。
4. 在 Supabase 后台确认写入成功，并验证公开网页无法读取历史答案。
5. 检查浏览器控制台无报错。

## 当前结论

文件结构、问卷内容、脚本语法和数据权限设计已达到试投放准备状态。完成 HTTPS 部署后的动态验收，才可标记为正式可投放版本。
